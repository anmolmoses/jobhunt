node_modules
.next
.git
.gitignore
.dockerignore
.env
.env*.local
.claude

# Data & uploads (mounted as volumes)
data
uploads
drizzle

# Lab host artifacts — must not leak into image. Volumes/bind-mounts create
# fresh mount points at these paths inside the container.
lab/node_modules
lab/.next
lab/data
lab/uploads
lab/.env
lab/.env.local

# Docker services
docker

# Internal docs
docs/internal

# IDE
.idea
.vscode
*.swp
*.swo

# Misc
.DS_Store
*.md
!package.json
LICENSE
